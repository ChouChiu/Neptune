services:
  neptune:
    build: .
    restart: unless-stopped
    env_file: .env
    network_mode: host
    volumes:
      - ./data:/app/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
